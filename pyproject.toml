[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "git-story"
version = "0.1.0"
description = "Human-readable narrative changelogs from git history using local LLMs"
requires-python = ">=3.11"
